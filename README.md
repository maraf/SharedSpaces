# SharedSpaces — Architecture & Implementation Plan

## Concept Summary

A self-hostable (or managed) web platform where users can join **shared spaces** via a QR code / URL+PIN pair, share files and text in real-time, with anonymous identity (display name only), JWT-based access, and WebSocket-driven live updates.

The **server** is a pure API with no rendered UI. The **client** is a separate SPA that can connect to any server — there is no deployment binding between them, and a single client instance may be connected to multiple servers simultaneously.

---

## Design Decisions

| Topic | Decision |
|---|---|
| IDs | All IDs are **GUIDs** |
| PIN lifecycle | Once a JWT token is created from a PIN, the PIN is **dropped** — used PINs are not stored |
| Token format | **JWT** with decodable claims: `display_name`, `server_url`, `space_id` |
| QR code format | Encodes the string `server_url:space_id:pin`; client app URL is independent of server URL |
| Display name | Client picks a display name **before joining** a space; it cannot be changed per-space; the UI supports defining a primary/current display name |
| Access type | **MVP: Read/Write for all members**; future versions may introduce configurable access levels |
| Server project | **Pure API** — no server-rendered UI |
| Space item GUIDs | **Generated by the client**, not the server |

---

## Tech Stack

- **Server:** .NET (ASP.NET Core) — pure Web API (no Razor Pages / MVC views)
- **Client:** React SPA (Vite) — independently deployable, connects to any server by URL
- **Real-time:** ASP.NET Core SignalR (WebSocket abstraction)
- **Database:** SQLite (self-host friendly, zero-config) with EF Core; swappable to PostgreSQL
- **File storage:** Local filesystem (self-host) with abstraction layer for future cloud storage

---

## Core Domain Model

All IDs are GUIDs.

### Space
| Field | Type | Notes |
|---|---|---|
| `Id` | GUID | Server-generated |
| `Name` | string | Human-readable name |
| `ServerUrl` | string | Canonical URL of this server instance |
| `CreatedAt` | datetime | |

### SpacePin
| Field | Type | Notes |
|---|---|---|
| `Id` | GUID | |
| `SpaceId` | GUID | FK → Space |
| `Pin` | string (hashed) | Short-lived; **deleted after token is issued** |
| `ExpiresAt` | datetime | |

### SpaceMember
| Field | Type | Notes |
|---|---|---|
| `Id` | GUID | Becomes the JWT `sub` claim |
| `SpaceId` | GUID | FK → Space |
| `DisplayName` | string | Set at join time; **immutable per space** |
| `JoinedAt` | datetime | |

### SpaceItem
| Field | Type | Notes |
|---|---|---|
| `Id` | GUID | **Client-generated** |
| `SpaceId` | GUID | FK → Space |
| `MemberId` | GUID | FK → SpaceMember (who shared it) |
| `ContentType` | string | `text` or `file` |
| `Content` | string | Text value or file reference path |
| `SharedAt` | datetime | |

---

## JWT Token Claims

The token is a standard JWT that the client can decode without a server round-trip:

```json
{
  "sub": "<member_id>",
  "display_name": "Alice",
  "server_url": "https://myserver.example.com",
  "space_id": "<space_guid>",
  "exp": 1234567890
}
```

The client extracts `server_url` and `space_id` from the token to know which server to call and which space to load — no separate state needed.

---

## Key Flows

### 1. QR Code / Join Flow

The QR code encodes the plain string: `server_url:space_id:pin`

Because the client app may be deployed at a completely different URL than the server, the QR code does **not** encode the client URL. Instead:

1. Space owner calls `POST /spaces/{spaceId}/pins` → server generates a short numeric PIN, stores it (hashed, with TTL), and returns the QR string `server_url:space_id:pin`.
2. The QR code is displayed by the client app. Scanning it on another device opens the **client app** (e.g. via a deep-link or well-known client URL), which then parses the three components.
3. Joining client shows a display name input. The user picks a display name (the UI allows setting a primary/default name to pre-fill this).
4. Client calls `POST /spaces/{spaceId}/tokens` (on `server_url`) with `{ pin, display_name }` → server validates PIN, creates a `SpaceMember`, issues a JWT, and **deletes the PIN**.
5. Client stores the JWT in local storage and uses it as a Bearer token for all subsequent requests to that server+space. Since the JWT contains `server_url` and `space_id`, no additional state is needed.

### 2. Multi-Server Client

- The client can hold JWTs for multiple server+space combinations simultaneously.
- The `server_url` claim in each JWT tells the client which base URL to use for API calls.
- Switching between spaces is purely client-side (no re-authentication needed if the token is still valid).

### 3. Sharing Content

- Text or file upload, stored with `SharedAt` + `MemberId` reference.
- The item's GUID is **generated by the client** and sent in the request (PUT/upsert semantics).
- Per-space storage quota enforced on upload.
- Flat list, ordered by `SharedAt`.
- **MVP access level: all members have Read/Write.** Future: configurable per-member or per-space roles.

### 4. Real-time Updates

- SignalR hub per space (`/hubs/space/{spaceId}`)
- On new item → broadcast to all connected members in that space.
- On item deletion → broadcast tombstone to all connected members.

### 5. Admin / Space Management

A privileged role (protected by a separate admin secret) that can:
- Create spaces
- Generate invitation PINs
- Remove space items

### 6. Offline Support (future-friendly)

- Client caches loaded items in IndexedDB.
- Service Worker intercepts uploads → queues them if offline → background sync when reconnected.

---

## API Endpoints

| Method | Route | Description | Auth |
|---|---|---|---|
| `POST` | `/spaces` | Create a new shared space | Admin |
| `GET` | `/spaces/{spaceId}` | Get space info | JWT |
| `POST` | `/spaces/{spaceId}/pins` | Generate a PIN (returns QR string `server_url:space_id:pin`) | Admin |
| `POST` | `/spaces/{spaceId}/tokens` | Exchange PIN + display name for a JWT; PIN is deleted | None |
| `GET` | `/spaces/{spaceId}/items` | List items in the space | JWT |
| `PUT` | `/spaces/{spaceId}/items/{itemId}` | Upsert an item (client-provided GUID) | JWT |
| `DELETE` | `/spaces/{spaceId}/items/{itemId}` | Delete an item | JWT |

---

## Project Structure

```
SharedSpaces/
├── src/
│   ├── SharedSpaces.Server/          # ASP.NET Core Web API + SignalR
│   │   ├── Domain/                   # Entities, value objects
│   │   ├── Features/                 # Vertical slice: Spaces, Pins, Tokens, Items, Admin
│   │   ├── Infrastructure/           # EF Core, file storage, SignalR hub
│   │   └── Program.cs
│   └── SharedSpaces.Client/          # React SPA (Vite) — independently deployable
│       └── src/
│           ├── features/             # join, space-view, admin
│           ├── components/
│           ├── hooks/                # useSignalR, useOfflineQueue
│           └── main.tsx
├── tests/
│   ├── SharedSpaces.Server.Tests/
│   └── SharedSpaces.Client.Tests/
└── docker-compose.yml                # For self-hosting
```

---

## Implementation Phases

### Phase 1 — Core Server
- Solution scaffold (`.sln`, projects, EF Core + SQLite)
- Domain entities + migrations
- Admin endpoints: create space, generate PINs
- Join endpoint: validate PIN → issue JWT → delete PIN
- Items endpoints: list, upsert (client-GUID), delete; enforce quota

### Phase 2 — Real-time
- SignalR hub: join space group, broadcast new/deleted items

### Phase 3 — React Client
- Vite + React scaffold
- Join flow: parse `server_url:space_id:pin` from QR or manual input; display name input (with primary name pre-fill)
- JWT storage + multi-server support
- Space view: flat item list, file/text upload
- SignalR client integration for live updates

### Phase 4 — Admin UI
- Simple admin panel: space creation, PIN generation

### Phase 5 — Offline & Polish
- Service Worker + IndexedDB for offline read/write queue
- Docker Compose for self-hosting
- QR code generation for `server_url:space_id:pin` strings

---

## Security Considerations

- JWTs are signed by the server; clients can decode claims (display name, server URL, space ID) without a round-trip but cannot forge them
- PINs are hashed at rest and deleted immediately after a token is issued — no replay possible
- Per-space storage quota prevents abuse
- Admin endpoints protected by a separate admin secret
- HTTPS enforced in production (standard ASP.NET Core middleware)
- Item GUIDs are client-generated; server must validate they belong to the correct space before accepting


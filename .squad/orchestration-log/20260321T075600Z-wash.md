# Orchestration Log: Wash (Frontend Dev)

**Timestamp:** 2026-03-21T07:56:00Z  
**Agent:** Wash (Frontend Dev)  
**Status:** SUCCESS

## Assignment

Create client CI/CD workflows (publish + deploy) and update vite.config.ts for version injection.

## Work Completed

### Files Created
1. **`.github/workflows/client-publish.yml`**
   - Triggers on `client-*` git tags
   - Builds client with `VITE_APP_VERSION` from tag
   - Zips dist/ artifact
   - Creates GitHub Release with `gh release create`

2. **`.github/workflows/client-deploy.yml`**
   - Manual trigger via `workflow_dispatch` with tag input
   - Reads CNAME from gh-pages branch to determine base path
   - Runs `vite build --base` for proper asset routing
   - Deploys to GitHub Pages via `actions/upload-pages-artifact` + `actions/deploy-pages`

### Files Modified
3. **`src/SharedSpaces.Client/vite.config.ts`**
   - Updated `define` option to prefer `process.env.VITE_APP_VERSION` env var
   - Falls back to `package.json` version if env var not set
   - Supports both tag-based (CI) and local development (package.json) workflows

## Validation

- **Lint:** ✅ PASS
- **Build:** ✅ PASS
- **Tests:** No new tests required (workflows are config; vite.config.ts change validated by build)

## Decision Propagation

Three user directives captured in `.squad/decisions/inbox/`:
1. Version injection is tag-based (git tag is source of truth), not package.json mutation
2. Client artifact is environment-agnostic; no server-url parameter in deploy
3. Deploy base path determined at deploy time from CNAME file, not hardcoded input

These decisions are canonical and should be preserved in team memory.

## Next Steps

- Decisions merged to `decisions.md` (Scribe responsibility)
- Workflows ready for PR review and merge to main
- Team can now use `git tag client-X.Y.Z && git push --tags` to trigger publish workflow

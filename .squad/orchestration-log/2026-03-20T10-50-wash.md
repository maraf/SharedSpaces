# Orchestration: Wash (Frontend Dev) — 2026-03-20T10:50

**Spawn Mode:** background  
**Task:** Replace connection status text pill with colored dot in space navigation pills  
**Outcome:** SUCCESS

## What Happened

Wash completed frontend refactoring to move connection status from space-view header to per-space navigation pills in app-shell.

### Changes Made

1. **Removed renderHeader()** from `src/SharedSpaces.Client/src/features/space-view/space-view.ts`
   - Eliminated separate status text pill display
   - Dispatch `connection-state-change` custom event on connectionState changes

2. **Added per-space dot rendering** to `src/SharedSpaces.Client/src/app-shell.ts`
   - Listen for `connection-state-change` events on `<main>`
   - Store connection state in `Record<string, ConnectionState>`
   - Render colored dot inside each space navigation pill
   - Dot colors: gray (no state), green (connected), orange (reconnecting), red (disconnected/error)

### Verification

- ✅ Lint passes
- ✅ Build passes
- ⚠️ Pre-existing test errors only (not caused by this work)

## Decisions Logged

- Event contract established: `connection-state-change` with `{ spaceId, state }` payload
- Dot color semantics defined in decisions.md

## Cross-Agent Impact

- **Zoe (Test Runner):** Tests referencing old `renderHeader()` or status text will need updates
- **All agents:** New event contract part of space-view API
